# Swift Code Review Template

Шаблон репозитория для автоматической проверки Swift проектов с помощью GitHub Actions.

## Что это?

Этот шаблон автоматически запускает **4 проверки** вашего Swift проекта:

1. **🔨 Проверка компиляции** - проверяет, что проект собирается без ошибок
2. **🎨 Проверка стиля кода** - анализирует код через SwiftLint
3. **🗑️ Поиск мертвого кода** - находит неиспользуемые классы и функции через Periphery
4. **💾 Проверка утечек памяти** - статический анализ retain cycles

Все проверки запускаются автоматически при каждом push и pull request.

## Как использовать?

### 1. Создайте новый репозиторий из этого шаблона

На GitHub:
1. Нажмите **"Use this template"** → **"Create a new repository"**
2. Укажите название вашего проекта
3. Выберите публичный или приватный
4. Нажмите **"Create repository"**

### 2. Клонируйте созданный репозиторий

```bash
git clone https://github.com/YOUR_USERNAME/YOUR_PROJECT.git
cd YOUR_PROJECT
```

### 3. Добавьте ваш Swift проект

Скопируйте файлы вашего проекта в репозиторий:

```bash
# Для Swift Package
cp -r /path/to/your/project/* .

# Для Xcode проекта
cp -r /path/to/your/project.xcodeproj .
cp -r /path/to/your/Sources .
```

### 4. Настройте SwiftLint (опционально)

Создайте `.swiftlint.yml` в корне проекта для настройки правил:

```yaml
# .swiftlint.yml
disabled_rules:
  - trailing_whitespace
line_length: 120
```

### 5. Сделайте commit и push

```bash
git add .
git commit -m "Добавлен проект"
git push
```

### 6. Проверьте результаты

Перейдите в **Actions** на GitHub и посмотрите результаты проверок:

```
https://github.com/YOUR_USERNAME/YOUR_PROJECT/actions
```

## Что проверяется?

### 🔨 Проверка компиляции

Проект должен успешно собираться. Поддерживаются:
- Swift Package Manager (Package.swift)
- Xcode проекты (.xcodeproj)
- Xcode workspaces (.xcworkspace)

### 🎨 Проверка стиля кода

Проверяет код на соответствие стандартам Swift через SwiftLint:
- Длина строк
- Именование переменных и функций
- Структура кода
- И многое другое

### 🗑️ Поиск мертвого кода

Находит неиспользуемые:
- Классы и структуры
- Функции и методы
- Свойства
- Протоколы

### 💾 Проверка утечек памяти

Статический анализ проблем с памятью:
- Retain cycles в closures
- Delegate без weak
- Stored closures без capture list

## Локальная проверка

Вы можете запускать проверки локально перед push:

### Установка инструментов

```bash
./scripts/setup.sh
```

### Запуск проверок

```bash
# Проверка стиля
swift-style-check .

# Поиск мертвого кода
swift-dead-code YourProject.xcodeproj

# Проверка памяти
swift-memory-check . --static-analysis

# Проверка компиляции
swift build  # для SPM
# или
xcodebuild -project YourProject.xcodeproj -scheme YourScheme build
```

## Структура файлов

```
your-project/
├── .github/
│   └── workflows/
│       └── code-review.yml      # GitHub Actions workflow
├── scripts/
│   └── setup.sh                 # Скрипт установки инструментов
├── Sources/                     # Ваш исходный код
├── Package.swift                # Если используете SPM
├── YourProject.xcodeproj/       # Если используете Xcode
├── .swiftlint.yml              # Конфигурация SwiftLint (опционально)
└── README.md
```

## Отчеты

Все отчеты сохраняются в **GitHub Actions Artifacts**:

1. Перейдите в Actions → выберите последний запуск
2. Прокрутите вниз до секции **Artifacts**
3. Скачайте отчеты:
   - `style-report` - отчет по стилю кода (JSON)
   - `dead-code-report` - отчет по мертвому коду (JSON)
   - `memory-report` - отчет по памяти (JSON)
   - `final-report` - сводный отчет (JSON)

## Интеграция с backend (для преподавателей)

Чтобы автоматически отправлять результаты на ваш backend:

1. Добавьте secrets в настройках репозитория:
   - `BACKEND_URL` - URL вашего API
   - `BACKEND_TOKEN` - токен аутентификации

2. Раскомментируйте код в `.github/workflows/code-review.yml`:

```yaml
# Найдите строки с комментарием "TODO: Раскомментируйте когда backend будет готов"
# и уберите комментарий (#)
```

3. Backend будет получать POST запрос с JSON:

```json
{
  "repository": "username/project",
  "branch": "main",
  "commit": "abc123...",
  "author": "student_name",
  "timestamp": "2024-01-01T12:00:00Z",
  "build_status": "success",
  "reports": {
    "style": { ... },
    "dead_code": { ... },
    "memory": { ... }
  }
}
```

## FAQ

### Проверки не запускаются?

1. Убедитесь, что в репозитории есть Swift файлы
2. Проверьте вкладку **Actions** - возможно они отключены
3. Убедитесь, что push был в ветку `main` или `develop`

### Проверка стиля находит слишком много проблем?

Настройте `.swiftlint.yml` - отключите ненужные правила:

```yaml
disabled_rules:
  - line_length
  - trailing_whitespace
```

### Нужны ли мне все проверки?

Нет, вы можете отключить ненужные jobs в `.github/workflows/code-review.yml`

### Где посмотреть детальные логи?

GitHub Actions → выберите запуск → нажмите на job → смотрите логи каждого step

## Поддержка

Если возникли проблемы:
1. Проверьте логи в GitHub Actions
2. Убедитесь, что все зависимости установлены
3. Обратитесь к преподавателю

## Лицензия

Этот шаблон распространяется свободно для образовательных целей.
