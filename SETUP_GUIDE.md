# Инструкция по развертыванию Template для преподавателей

## Шаг 1: Создание Template Repository

### 1.1. Создайте новый репозиторий на GitHub

```
Название: SwiftProjectTemplate (или любое другое)
Описание: Template for Swift projects with automated code review
Public/Private: Public (чтобы студенты могли использовать)
```

### 1.2. Скопируйте файлы из `template/` в новый репозиторий

```bash
cd template
git init
git add .
git commit -m "Initial template setup"
git branch -M main
git remote add origin https://github.com/YOUR_ORG/SwiftProjectTemplate.git
git push -u origin main
```

### 1.3. Сделайте репозиторий Template

1. Перейдите в **Settings** репозитория
2. Отметьте галочку **"Template repository"**
3. Сохраните

Теперь у репозитория появится кнопка **"Use this template"**!

---

## Шаг 2: Обновление ссылок на Code Review Tools

В файле `.github/workflows/code-review.yml` найдите строку:

```yaml
git clone https://github.com/${{ github.repository_owner }}/SwiftCodeReviewTools.git tools
```

Замените `SwiftCodeReviewTools` на название вашего репозитория с инструментами.

Также обновите `scripts/setup.sh`:

```bash
# Было
git clone https://github.com/YOUR_ORG/SwiftCodeReviewTools.git "$TOOLS_DIR"

# Должно быть
git clone https://github.com/YOUR_REAL_ORG/SwiftCodeReviewTools.git "$TOOLS_DIR"
```

---

## Шаг 3: Настройка отправки результатов на backend

### 3.1. Подготовьте backend endpoint

Создайте API endpoint, который будет принимать POST запросы:

```
POST /api/code-review/results
Content-Type: application/json
Authorization: Bearer YOUR_TOKEN
```

Пример payload:

```json
{
  "repository": "student_username/project_name",
  "branch": "main",
  "commit": "abc123def456...",
  "author": "student_username",
  "timestamp": "2024-01-15T10:30:00Z",
  "build_status": "success",
  "reports": {
    "style": {
      "toolName": "Swift Style Check",
      "filesChecked": 42,
      "summary": {
        "errors": 5,
        "warnings": 23,
        "infos": 10
      },
      "issues": [...]
    },
    "dead_code": { ... },
    "memory": { ... }
  }
}
```

### 3.2. Раскомментируйте код в workflow

В `.github/workflows/code-review.yml` найдите секцию:

```yaml
# TODO: Раскомментируйте когда backend будет готов
# if [ -n "${{ secrets.BACKEND_URL }}" ]; then
#   curl -X POST ${{ secrets.BACKEND_URL }} \
#     -H "Content-Type: application/json" \
#     -H "Authorization: Bearer ${{ secrets.BACKEND_TOKEN }}" \
#     -d @final-report.json
# fi
```

Раскомментируйте эти строки (уберите `#`).

### 3.3. Добавьте secrets в Template Repository

**ВАЖНО:** Secrets нужно добавить в каждый репозиторий студента, НЕ в template.

Для студенческих репозиториев:

1. Settings → Secrets and variables → Actions
2. Добавьте:
   - `BACKEND_URL` = `https://your-backend.com/api/code-review/results`
   - `BACKEND_TOKEN` = `your_secret_token_here`

**Альтернатива:** Используйте Organization secrets, если студенты в одной организации.

---

## Шаг 4: Инструкция для студентов

### 4.1. Создайте отдельный документ для студентов

Скопируйте `README.md` и дополните инструкциями:

1. Как создать репозиторий из template
2. Как добавить свой проект
3. Где смотреть результаты
4. Что делать если проверки не проходят

### 4.2. Проведите демонстрацию

Покажите студентам:
1. Как использовать template
2. Где смотреть результаты в GitHub Actions
3. Как читать отчеты
4. Как исправить типичные ошибки

---

## Шаг 5: Мониторинг результатов

### 5.1. Через GitHub API

Вы можете получить результаты через GitHub API:

```bash
# Получить список workflow runs
curl -H "Authorization: token YOUR_GITHUB_TOKEN" \
  https://api.github.com/repos/STUDENT_USER/STUDENT_REPO/actions/runs

# Скачать артефакты
curl -L -H "Authorization: token YOUR_GITHUB_TOKEN" \
  https://api.github.com/repos/STUDENT_USER/STUDENT_REPO/actions/artifacts/ARTIFACT_ID/zip \
  -o report.zip
```

### 5.2. Через backend

Если настроили отправку на backend, все результаты будут автоматически приходить туда.

---

## Частые проблемы и решения

### Проблема: Workflow не запускается у студентов

**Решение:**
1. Убедитесь что Actions включены: Settings → Actions → Allow all actions
2. Проверьте что есть push в ветку `main` или `develop`
3. Убедитесь что в репозитории есть `.github/workflows/code-review.yml`

### Проблема: Инструменты не собираются

**Решение:**
1. Проверьте что URL репозитория с инструментами правильный
2. Убедитесь что репозиторий публичный или студент имеет доступ
3. Проверьте что `Package.swift` корректный

### Проблема: SwiftLint находит слишком много проблем

**Решение:**
1. Ослабьте правила в `.swiftlint.yml`
2. Научите студентов настраивать конфигурацию под свой стиль
3. Используйте `--strict false` или отключите строгий режим

### Проблема: Periphery не может найти схему

**Решение:**
1. Студент должен указать схему явно в проекте
2. Или использовать флаг `--scheme SchemeName` в workflow
3. Или пропустить проверку мертвого кода для SPM проектов

---

## Дополнительные настройки

### Добавление тестов

Добавьте job для запуска тестов в `.github/workflows/code-review.yml`:

```yaml
test:
  name: 🧪 Запуск тестов
  runs-on: macos-latest
  steps:
    - uses: actions/checkout@v4
    - name: Запуск тестов
      run: swift test
```

### Добавление code coverage

```yaml
- name: Code Coverage
  run: |
    swift test --enable-code-coverage
    xcrun llvm-cov export -format="lcov" \
      .build/debug/YourPackageTests.xctest/Contents/MacOS/YourPackageTests \
      -instr-profile .build/debug/codecov/default.profdata > coverage.lcov
```

### Интеграция с Codecov

```yaml
- name: Upload coverage to Codecov
  uses: codecov/codecov-action@v3
  with:
    file: ./coverage.lcov
```

---

## Масштабирование

### Для большого количества студентов

1. **GitHub Organization**
   - Создайте организацию для курса
   - Используйте Organization secrets
   - Автоматизируйте создание репозиториев через GitHub API

2. **Backend для сбора результатов**
   - База данных для хранения всех отчетов
   - Dashboard для мониторинга
   - Система оценки на основе метрик

3. **Автоматизация**
   - GitHub Classroom для автоматического создания репозиториев
   - Webhooks для real-time уведомлений
   - Auto-grading на основе результатов проверок

---

## Контакты и поддержка

При возникновении вопросов:
1. Проверьте логи GitHub Actions
2. Изучите документацию инструментов (SwiftLint, Periphery)
3. Обратитесь к сообществу GitHub

Успехов в обучении студентов! 🎓
